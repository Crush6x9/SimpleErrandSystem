<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.delivery.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.errand.entity.User">
        <id     property="id"           column="id" />
        <result property="phone"        column="phone" />
        <result property="password"     column="password" />
        <result property="role"         column="role" />
        <result property="studentId"    column="student_id" />
        <result property="idCardImage"  column="id_card_image" />
        <result property="verified"     column="verified" />
        <result property="createdTime"  column="created_time" />
        <result property="updatedTime"  column="updated_time" />
    </resultMap>

    <insert id="insert" parameterType="com.errand.entity.User" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO `user` (
            phone, password, username, avatar,
            student_id, certificate, role,
            create_time, update_time
        ) VALUES (
                     #{phone}, #{password}, #{username}, #{avatar},
                     #{studentId}, #{certificate}, #{role},
                     #{createTime}, #{updateTime}
                 )
    </insert>

    <select id="findByPhone" parameterType="string" resultMap="UserResultMap">
        SELECT
            user_id, phone, password, username, avatar,
            student_id, certificate, role,
            create_time, update_time
        FROM `user`
        WHERE phone = #{phone}
    </select>

    <update id="update" parameterType="com.errand.entity.User">
        UPDATE `user`
        SET
            phone = #{phone},
            username = #{username},
            avatar = #{avatar},
            student_id = #{studentId},
            certificate = #{certificate},
            role = #{role},
            update_time = #{updateTime}
        WHERE user_id = #{userId}
    </update>

    <update id="updateRole">
        UPDATE `user`
        SET
            role = #{role},
            update_time = #{updatedTime}
        WHERE user_id = #{id}
    </update>

    <update id="updateVerification">
        UPDATE `user`
        SET
            student_id = #{studentId},
            certificate = #{idCardImage},
            update_time = #{updatedTime}
        WHERE user_id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE `user`
        SET
            password = #{password},
            update_time = #{updatedTime}
        WHERE user_id = #{id}
    </update>

</mapper>